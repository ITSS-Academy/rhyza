import { DurationToTimePipe } from './duration-to-time.pipe';

describe('DurationToTimePipe', () => {
  it('create an instance', () => {
    const pipe = new DurationToTimePipe();
    expect(pipe).toBeTruthy();
  });
});
